Agora ops + distribution-watch loop (resume 2026-06-29). PASTE AS /loop in a fresh session. Chat with the owner in SLOVAK; all code + public output in ENGLISH. FIRST read C:\Users\Danculus\agora\HANDOFF_CURRENT_STATE.md fully, then proceed. Self-pace ~1800s; each wake do the routine below, then ScheduleWakeup or the loop dies.

EACH CYCLE:

1) HEALTH (verify only, do NOT relaunch unless actually dead). Exactly ONE of each: brain agora.main (one :8000 LISTEN), dungeon mcp_server.py (:5174), dungeon_canary.py, agent_activity_monitor.py, self_improvement_controller.py.
   - curl -s http://127.0.0.1:8000/api/v1/health  -> {"status":"ok",...}
   - python tools/dungeon_health.py 20 3  -> loop_n MUST advance (HTTP 200 LIES; never trust it alone).
   - Production is bursty BY DESIGN (~39 findings/day). Do NOT churn or "fix" production on a quiet hour — that was the 2-week false-alarm trap ([[busy-but-idle-was-miscalibrated-monitor]]). "LLM_quests=0 (planner OFF)" is intentional.
   - If a process is genuinely dead, relaunch only that one per HANDOFF.md section Relaunch, then re-verify 200 + ONE :8000 + ONE mcp_server.py + ZERO supervisors.

2) REDDIT WATCH (read-only). Threads to check for NEW external replies (commenter != Danculus/DanceNitra):
   1ui031b (overconfidence post), 1uhajcp (memory-poisoning), 1ugb45a (r/Rag), 1uhal5r (r/AIMemory).
   Use tools/distribution_radar._reddit_token -> GET https://oauth.reddit.com/comments/<id>?limit=100. CRITICAL: WALK THE FULL NESTED t1 REPLY TREE (replies live under .data.replies), sort by created_utc, and check the LAST author PER THREAD (same last-author discipline as the GitHub watch). A flat list of external top-level authors LIES — it ignores our nested replies and times, which once produced a false "unanswered" alarm (we were actually the last commenter). Only if the newest comment is THEM (not Danculus/DanceNitra) AND it is a real question/challenge -> draft a SLOVAK briefing (who / what they said / our proposed answer / how we use it) + a SHORT HUMAN draft (2-4 plain sentences, no over-framing, verify attribution, own mistakes — [[reddit-replies-must-be-human-short]]). GATED: owner posts Reddit MANUALLY, never auto-post. NOTE: carc's "feels like an LLM" jab on 1uhajcp is deliberately LEFT ALONE (Neeraj addressed it; owner agreed not to pile on).

3) GITHUB WATCH (outreach backstop — do NOT rely on Envoy alone). POST /brain/envoy/sweep, then for EACH tracked thread fetch real comments (api.github.com/repos/<repo>/issues/<n>/comments) and check the LAST author. If the newest is the OTHER party -> UNANSWERED -> gated Slovak briefing + draft, Telegram the owner. Tracked (check last-author each cycle; ALL currently = us/ours, balls on their side): Elina-Seed #47 (qingkong66 — we posted the drop-in Supersession spec; he's folding it into the Elina spec), agora **Discussion #2** (Neeraj/MemStrata — we posted the equal-peer reply + schema_v0; via GraphQL: `gh api repos/DanceNitra/agora/discussions/2/comments --jq 'last|.user.login'`), hermes #10771, openclaw 35203, deepseek #1121 (IGNORE the /attempt PR-claim). ALSO WATCH (do NOT post — go via qingkong): **deepseek #1447/#1462** = qingkong66's cross-framework memory-validation effort (Type-B scenarios fit mnemo); participate THROUGH our collaborator, never as a rival organizer. Never say "outreach caught up" from the inbox alone — verify last-author per thread. Be fair, never snub ([[collaboration-fair-never-snub]]).
   - ACTIVE NON-LOOP TASK (separate from this watch): the professional audit of all 42 published posts via the **/audit-post** skill — state in HANDOFF_CURRENT_STATE.md + agora_output/publish_audit_tracker.md; NEXT = #3. The loop only watches; the owner drives the audit with a separate prompt.
   - SCOUT DRAIN (keeps the GitHub scan alive): the dungeon only queues a new Scout scan when NO "Scout outreach" task is pending, so ONE unprocessed Scout task blocks ALL future scans -> scout goes stale. EACH cycle, if a "Scout outreach" inbox task is pending, judge the fit HONESTLY (does our vault answer it with a real mechanism + a measured number?); if yes draft GATED, else POST /brain/scout-record {url,repo,issue,outcome:'no real fit'} and mark done. Recording (even a no-fit) refreshes scout last_scan and unblocks the next scan. Reputation dies on a bad pitch — no overselling a partial fit.

4) INBOX (optional, only if genuinely idle): GET /brain/claude-inbox (read .pending, print COUNT). Handle ONE highest-value ON-FRONTIER task with the severe-test rule (real Lab run + falsifier in the same cycle; POST /brain/claude-inbox/done {id,result}). Skip duplicate/saturated/off-Board via POST /brain/gatekeeper/skip. Vault notes ONLY via DUNGEON_AUTOPUSH=1 python tools/safe_vault_push.py "..." (additions-only, NEVER git add -A). Else DO NOTHING.

5) TELEGRAM: ONE Slovak line ONLY for a genuine breakthrough or a breakage (token from server/.env via python -X utf8 — never echo the literal token). Stay silent on routine cycles.

HARD RULES: outreach/press/posts GATED (Slovak briefing first, owner approves; Reddit manual). PUBLISH GATE — before ANY Crucible entry / post / press / outreach that asserts a finding: run /stress-claim (the argument — textbook? overclaim? confound? falsifier real? -> PUBLISH/REFRAME/KILL) AND /verify-claims (every cited number+source vs its PRIMARY source -> banner + corrections, our own Lab numbers too); apply the fixes (REFRAME/KILL or correct) and only THEN render + push. Verify measured numbers vs the source lab before any public citation. Never commit server/.env or any sk-ant key. Public commits use anon identity agora-builder@users.noreply.github.com + leak-scan the diff first. Small reversible separate commits; py_compile + both servers 200 + ONE :8000 before any server change; revert on breakage; at most ONE self-upgrade/cycle. Local GPU is too slow — ONLY embeddings local, all LLMs cloud, never recommend local LLM. Then ScheduleWakeup ~1800s and stop.
