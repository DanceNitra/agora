folded-scalar claim does not reproduce on Claude Code CLI 2.1.272 win32: five spellings of one 168-char description, project and user tiers, 10 of 10 arrive in full, read from the request body; probes/does_a_folded_scalar_description_arrive_at_the_model.py in DanceNitra/agora
the docs give the skill listing a character budget of 1% of the context window; over budget every name is kept and descriptions drop least-used first; SLASH_COMMAND_TOOL_CHAR_BUDGET overrides it
42,207 tokens for 189 skills is about four times over the budget on a 1M window and twenty times over on 200k, if that figure is the on-disk total; if it is what the model received, no budget fired and it is a different loader
a discriminating test: set SLASH_COMMAND_TOOL_CHAR_BUDGET to a character count larger than the whole listing, change no YAML, fresh session, count again
tonydzi wrote that the two routes are indistinguishable from the author's chair; the reply credits that as his point
