# humanizer skill on the #91188 reply

Voice calibrated against our own published comments in the same repository, read before rewriting
(anthropics/claude-code#82056, author DanceNitra). That voice leads with the finding, uses run-in
bold headings, admits limits inline, and signs off by name.

## What the pass changed

1. OPENING. It announced our arrival before answering anyone. Now it answers the question the two
   of them asked each other, in the first sentence.
2. A LECTURE REMOVED. A whole paragraph on minified identifiers closed on "Quote what it does, never
   what things are called." An aphorism aimed at nobody. The fact survives as one clause inside the
   limits paragraph, where it is a caveat rather than a lesson.
3. PERFORMED RIGOR. "It called Python's len() a UTF-16 count, and len() returns code points ... so it
   was exact and every run agreed with itself." Placed beside a collaborator's self-correction that
   flipped his conclusion, ours flipped nothing. Cut.
4. SCOREBOARD TONE. The credit correction opened the letter. Moved out, and the paragraph addressed
   to @tonydzi now leads with the source vindicating his hedge.
5. ANTHROPOMORPHISM. "the label that does not reach you" and "the reminder tells you" reduced to
   what the code renders.
6. RULE OF THREE. "Three layers, none of them bytes" removed with the paragraph it closed.
7. STRUCTURE. Run-in bold headings added to match the published voice, one per finding.
8. SIGN-OFF added, matching the two prior comments.

## Constraints held

Pure ASCII, no em dash or en dash, no curly quotes. The code block is verbatim from the shipped
bundle. Twelve strings the verification probe is keyed to survived unchanged. "UTF-16" appears once,
as a reference rather than a finding.

## Still-AI check on the final text

Two prose sentences reach 26 words and both carry one idea. No signposting, no generic conclusion,
no boldface used for emphasis rather than as a run-in heading.
