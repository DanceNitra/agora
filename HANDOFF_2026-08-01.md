# HANDOFF 2026-08-01 — the day five confident conclusions were wrong, and the gate caught four

Written for a cold restart. Read §0 and §1 first; everything else is reference.

---

## 0. DO THIS FIRST, before anything else

**Restart the MCP connection (`/mcp`, or the fact that this is a new session may already have done it).**
The `inspeximus-mcp` process that served the previous session had been alive since **2026-07-24** and was
pinned by `uvx` to **inspeximus 1.46.0** — a build from before the single-writer guard shipped (1.58.0).
It destroyed 290 records of the memory store, twice, silently. Details in §5.

Verify it took: `mcp__inspeximus__memory_report` should say **297**, not 6 or 8. If it says 6 or 8, the
old process is still serving and **nothing may be written through it**.

Health check both servers (they were up and untouched at handoff: brain 8 agents, dungeon 200):

```bash
curl -s http://127.0.0.1:8000/api/v1/health
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:5174/
```

**The working tree is on branch `integration/dungeon-alpha-omega`, NOT `main`.** It is 80 commits and
~17,300 lines ahead and deliberately unpushed. `git push origin main` will say "up-to-date" and mean
nothing. See §3.

---

## 1. What is OPEN and needs the owner's word

| # | item | state |
|---|---|---|
| 1 | **The 80-commit integration branch** | Complete, unpushed, unmerged. The batch plan says explicitly: nothing outward, present the diff for approval. Acceptance bar `probes/swarm_health.py` is **7/8, exit 1**. |
| 2 | **King Aldric fails the swarm bar** | 0 decisive outcomes in 24h. Not a defect: his forecasts mature on a 21–120 day horizon and the bar is daily. Needs a decision on the criterion for a long-maturation organ, not a code change. `probes/forecaster_calibration.py` measures him on his own terms. |
| 3 | **Gated press proposal `9d4b42`** | Waiting. |
| 4 | **Whalebone filter policy** | Allow `polymarket.com`, or keep the Oracle on play-money Manifold. |
| 5 | **Stale-process probe** | Offered, not built: a few lines reporting each long-lived process's ACTUAL version against the latest. Would have caught §5 eight days early. `witness_server` has been up since 2026-07-25 and is in the same class. |

---

## 2. What SHIPPED today, verified

**inspeximus 1.89.0 — on PyPI, installed from PyPI in a clean venv and exercised there.**
Two correctness fixes on the retraction path plus a new scope:
- `slash()`/`restore()` now force the write. Before, they used the 5-second throttled `_save()`, so a
  retraction could be lost to a process restart while the caller was told `{"slashed": 1}`.
- `slash(scope='source')` walked a `taint` set frozen at write time, so a `derived_from` edge arriving
  later was invisible. Both operations now close forward over declared edges, mirrored in `restore()`.
- New `scope='lineage'`; unresolvable parents kept as `derived_from_unresolved` instead of dropped.
- Gate passed in full: suite **1821**, mutation check **155/155 killed, 0 survived, 0 skipped**.

**Four public GitHub comments, each read back after posting:**
- `luoxuejian000/edrn-dmrg-verification#1` [5150494894] — arXiv endorsement: it is per-domain, none of
  the three proposed people has a `cs.*` paper, and one ("Guido de Groot") has no trace in arXiv, ORCID,
  Semantic Scholar, OpenAlex, INSPIRE-HEP or Crossref.
- `#2` [5150703763] — the folding check, plus `warm_start_time_arrow.py`. His "time is static" claim was
  untestable as written (the scan recomputes every point from scratch, so order cannot matter); the
  warm-started version CAN fail and it **confirms him**: 1.3e-15 at N=8 where there is no degeneracy.
- `openclaw/openclaw#7707` [5150877282] — icophy is right for every real user, plus a bug they had not
  seen: `MemorySearchResult.originClass` has no writer anywhere in the repo but IS read by a live gate
  at `extensions/active-memory/trigger-recall.ts:80`, so trusted-origin entries outside MEMORY.md and
  USER.md are silently excluded from trigger-recall.
- `#1` [5150994997] — Zenodo confirmation to Guanghao: publish now, the journal is the path.

**A Crucible entry, live at `dancenitra.github.io/agora/public/crucible/`** and verified by fetching the
deployed page: a FAILED replication of **our own** feature. An inferred lineage edge recovers at most
13.3% of true derivations (0.083 at 70% wording retained, 0.017 at 50%, 0.000 at 30%); precision 1.000
and zero false positives, so the failure is recall. Lab `a1d88c`. Cherry-picked to `main` alone —
`d00a98f` — without the other 80 commits.

**The memory store**: 289 file notes backfilled into inspeximus (297 records), and CLAUDE.md rule #14
now requires every memory write to go to both stores.

---

## 3. The branch situation, precisely

```
integration/dungeon-alpha-omega   80 commits ahead of origin/main, 85 files, +17,315 / -461
origin/main                       d00a98f (the Crucible entry, cherry-picked alone)
GitHub Pages deploys from main ONLY
```

Do not `git checkout` another branch in `C:/Users/Danculus/agora` — the brain and dungeon run from it and
it would swap 85 files of code under live processes. Use `git worktree add` at a **short** path
(`/c/wtmain`); under the scratchpad the checkout dies on Windows' 260-character limit against the repo's
long asset filenames.

---

## 4. Threads awaiting a reply from THEM

- **EDRN #2** — seven questions to Guanghao, unanswered: `dmrg_backward.csv` is an exact row-reversal of
  `dmrg_forward.csv` (difference `0.000e+00` on all four columns); the 6.4e-02 DMRG spread is initial
  state, not order (`use_random_init=True` with `conserve='Sz'` puts each point in a different
  magnetisation sector — 39/39 predicted sectors match his own `coarse` column); the five
  `memory_fold_alpha*.csv` are byte-identical so alpha is inert; `negative_1`/`negative_2` are the first
  15 and 20 rows of the eps=0.00 run; `gap`/`coarse`/`fine` are a per-edge lookup with no history
  dependence; `coarse` is zero by Sz symmetry; `random_scan_results.csv` has 6 of 13 gap values negative.
  **Whether he runs the script and answers these is the test of whether this is a collaboration.**
- **r/RAG** — the chi-square measurement jacksonxly asked for was run and the draft was posted by the
  owner. Result: he is right and more strongly than he put it. No non-normal pool converges to nominal;
  the lognormal arm gets **worse** with n (63.4% at n=5 down to 30.8% at n=320) because the interval
  tightens around an `s` that almost never sampled the tail. Our published 94.2%/96.4% reproduce (95.2
  and 96.3) and do not generalise. Script: `scratchpad/chisq_across_n.py`.
- **openclaw #7707** and **DeepSeek #1462/#1466** — watch for replies.

---

## 5. The MCP data loss, resolved

The `inspeximus-mcp` process was started **2026-07-24 22:29** and pinned by `uvx` to the cache archive it
resolved that day:

```
version 1.46.0
_file_sig                ABSENT
StoreChangedOnDisk       ABSENT
```

The single-writer guard shipped in **1.58.0**. One ordinary `remember_decision` through that server took
the store from 294 records to 7; repeated deliberately, 298 → 8. **The library is not defective** — the
guard fires correctly cross-process, through `open_store()`, and through the real `mcp_server` module's
`_MEM`, on both 1.88.1 and 1.89.0. Six reconstructions failed to reproduce it because all six ran on a
build that has the fix.

Backups: `~/.inspeximus/mcp_memory.json.bak-{before-backfill,after-clobber,before-seq-test}-20260801`.

---

## 6. Five conclusions I stated today that were wrong

Kept because the pattern is identical in all five: the arithmetic was right and the error was one storey
up, in what the numbers measured.

1. **"All three LLM tiers are dead."** My probe ran from the repo root and never loaded `server/.env`, so
   it measured the code's built-in default model. All three answer.
2. **"The MCP server does not clobber."** A correction to a correct first instinct, from a probe that
   measured the wrong build. See §5.
3. **"Guanghao's central result fails."** I joined `ed_backward.csv` row-by-row against `ed_forward.csv`,
   which is written in traversal order, so I compared s=0 against s=3. Joined on the value of s the
   difference is 1.5e-15.
4. **"icophy is wrong about `memory_search`."** True on `main`, false for every user: the fix landed 16
   hours before he wrote, in no installable build, and npm `latest` still does not carry it.
5. **A Lab model whose `VERDICT` line contradicted its own `MEASURED` line** — a bag-of-words cosine over
   a 14-word vocabulary returned recall 1.000 while the prose asserted "a small fraction". The verdict
   had been written before the number.

**Four of the five were caught because the owner asked for a skeptic/validator/auditor panel.** The one
that was not (#5) was caught by re-reading the output. New standing rules from this: run a **positive
control** before any zero-result claim; **compute** a verdict from parsed numbers rather than writing it;
and establish **what production actually executes** before explaining its failure.

---

## 7. Where things live

- Brain `server/`, dungeon `agora-game-server/`, both running from the repo, both current.
- inspeximus dev tree: `C:/Users/Danculus/inspeximus-repo` (v1.89.0 tagged and pushed).
- Memory: `~/.claude/projects/C--Users-Danculus-agora/memory/` (290 notes) + the inspeximus store.
- Today's scratchpad: `chisq_across_n.py`, `edrn_fold/` (the folding check and the warm-start script),
  `backfill_memory_to_inspeximus.py`, `repro_clobber/`, `verify_1890_from_pypi.py`.
