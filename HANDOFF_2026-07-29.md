# HANDOFF — 2026-07-29

Read this, then `agora_output/ORGAN_REDESIGN_PLAN.md`. Everything below is measured, not remembered.

---

## THE ONE LESSON OF THE DAY

**Four times I reported an agent as producing nothing, and four times it was my instrument, not the
agent.** Wren (11 decisive, read as 0), Orin (10, read as 1), Elara (93 in a day, read as 0), and
"Mira produces 32 with 0 decisive" — which turned out not to be her store at all. Every miss came
from writing a vocabulary list from memory instead of opening a store that was sitting on disk.

The rule now lives in `server/agora/execution/repair_ledger.py`: **before adding an organ, open its
store, run a Counter over its outcome/status field, and take ITS words.**

Second lesson, same shape: I fixed a "selector can't advance past a rejected item" deadlock in the
morning and **shipped the identical defect in my own new code six hours later.** That is why
`research/probes/advance_audit.py` exists — it caught it.

---

## WHAT IS FIXED AND VERIFIED (all committed)

| what | commit | evidence |
|---|---|---|
| arXiv OR-firehose closed at the single choke point | `4639e0c` | 4/8 → 8/8 on-mission, controls unchanged |
| Library loop drains the reading list; stale queue pruned | `8e948d8` | 110 of 199 stale entries removed |
| Challenge sweep walks a candidate list | `2c03274` | *kept, but did NOT fix the outage — see refuted* |
| **REPAIR LEDGER** — the second book beside note counts | `d437989` | Rooke was #1 all along, scored 0 by note count |
| `owed` warning on inbox close | `0212060` | + three retracted diagnoses |
| **Pending-guard expires after 36h** | `1d2109e` | Kael's triage unblocked after 29.5h stuck |
| Cadence heartbeat (1.05s/tick measured) | `b18ab9f` | retracts my "off by 25x" claim |
| **Completion gate REFUSES a close with no ledger entry (409)** | `784f9c9` | tested both ways |
| Elara stops judging our own vault-digests | `38d76fe` | + Wren's vocabulary |
| **Bridge bench** — Wren's 69 hypotheses get a consumer | `ff0818d` | ids backfilled onto 80 charts |
| Starvation alarm covers **8/8 agents** | `4b30eb1` | was 5/8; mtime fallback added |
| "compatible" is a verdict — Elara 0 → 93 decisive | `4381687` | org total 4 → 97/day |
| `.contributions` is the SEMINAR, not Mira's | `06d7d90` | all 8 are `partners` on every record |
| **Seminar verification unstuck** | `48ee5b0` | **0% → 93.8%**, 1094 contributions verified |
| Advance-audit sweep + the 4th instance (mine) | `73609a6` `a080d0a` | forge/academy/hypothesis read and cleared |
| Erasure cell v2 with real controls | `b1d3443` | **on the matched operation we TIE** |
| Crucible method doc | `28497c4` | first publishable non-verdict artifact |

**Both servers verified up after every restart: brain 200 with exactly one `:8000` listener, dungeon
200 with exactly one `mcp_server.py` and zero stray supervisors.**

---

## WHAT I CLAIMED AND HAD TO RETRACT — do not rebuild on these

1. "Two relevance filters disagree, the head wedges" — the gate passes **8/8 including the head**.
2. "The gate over-rejects" — it accepts **36/69 (52%)** of the canon; its tokens are generic English
   (`bed`, `better`, `answer`, `buyer`), so it barely filters at all.
3. "`loop_n` resets on restart" — it does not; it is restored from the heartbeat file (2026-06-19).
4. "Cadence is off by 25x" — measured **1.05s/tick**, off by 24%. I derived the first number from
   `[plan]` log lines, which are planner events, not loop iterations.
5. "The Library fallback caused the 202 off-mission papers" — refuted by measurement; under
   `sort='relevance'` even the bare phrase returns 8/8 on-mission.
6. "HeartFlow archives rather than deletes" — I reasoned from `src/memory/forgetting.js`, which
   **nothing in `src/` imports**. Dead code. The wired path deletes, and it encrypts at rest.

**The actual cause of the 42-day Bounty/Court + Graveyard outage was me:** three `Challenge belief`
tasks queued 07-20/21 were marked done with an empty `result` and the verdict never posted. That is
what `784f9c9` now refuses.

---

## OPEN — pick up here

### Immediate, concrete
- **Inbox: 16 pending.** Recommended order: the five memory leads as ONE batch (`e5d955` Does Memory
  Credit Travel, `5d031f` Contamination Percolation, `875366` attack surface, `5278d3` Layered
  Epistemic, `9429a6` graph serialization) → then `4d4370` (llama_index#21666 memory-poisoning
  defense — the only task with an open third-party issue we can answer with evidence) → then
  `1ad4e4` (roadmap panel; I wrongly filed it as routine this morning and it was the one instrument
  showing the 42-day silence).
- **`ORGAN_REDESIGN_PLAN.md` Phase 3 is unstarted.** The v1 beat table is filled and gated. One organ
  per commit, measured before and after, no organ ships without its starvation signal wired.
- **Still starving:** cartography (Wren) and analogies (Orin) — both got a consumer today and are
  waiting on their first cycle. Check `GET /brain/repair-ledger`.

### Awaiting an external reply
- **yun520-1 / HeartFlow** — asked publicly (DeepSeek#1462) for a private channel. Four findings are
  written and held: a data-loss path (the `.aes-key` guard scans `memory/` while the bank lives in
  `data/`), `encryptJSON`'s silent PLAINTEXT_FALLBACK, the async key path writing 0o666 while logging
  "(0o600)", and `_ForgettingEngine` never being declared. **Do not post these publicly.**
- **Saravanan / world-model-mcp** — issue #37 opened with the measurement. Question asked: is
  retrievability-after-delete intended?
- **Marat / EDRN** — owner sent my review. Point 4 (the error/p confound) has now been skipped
  **twice**. Spearman(norm_error, p) = −0.49 within the odd arm, −0.83 across all. One more round,
  then say plainly that this has become unpaid review in a field that is not our frontier.

### Unresolved
- `hypothesis_induction` avoids the deadlock by **never remembering** — it re-samples randomly, so a
  good theme rejected once is lost forever. Opposite failure, not fixed.
- The Seminar produced 32 contributions in the last 24h with **0 verified** at the time of the fix;
  after unsticking, recent verification runs at 93.8%. Watch whether new contributions verify.
- HeartFlow's `MemoryBank` is constructed at `heartflow.js:1402` but **no method on it is called
  anywhere**; `memory-kernel.js` does real-time plaintext persistence that `forget()` never touches.
  Unsettled — do not publish any HeartFlow comparison until it is.

---

## STANDING CONSTRAINTS (unchanged, do not relearn the hard way)

- Vault is fragile: **never** `git add -A` / `git reset` in `my-second-brain`; push via
  `tools/safe_vault_push.py`.
- Outreach is GATED — Slovak briefing to the owner first, always.
- Secrets stay in gitignored `.env`; never echo the Telegram token on a command line.
- One `:8000` listener, one `mcp_server.py`, zero stray supervisors — verify after every restart.
- **Never `until … do sleep … done` on a log file you might rewrite.** Three such loops from previous
  sessions were still spinning today; all killed.
- The owner rejected "exclude inspeximus from the leaderboard" as an evasion, and he was right. The
  fix is reproducibility: public re-runnable harness, numbers produced in CI on neutral
  infrastructure, competitors free to challenge any row including ours.
